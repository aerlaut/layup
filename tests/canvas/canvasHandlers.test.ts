/**
 * canvasHandlers tests — pending Task 05 (canvas interaction handler rewrite).
 *
 * The handlers in canvasHandlers.ts and canvasDragDrop.ts depend on the old
 * DiagramState shape (navigationStack, diagrams map, childDiagramId, etc.).
 * These tests will be rewritten as part of Task 05.
 */
import { describe, it } from 'vitest';

describe('canvasHandlers', () => {
  it.todo('handleConnect — Task 05: rewrite canvas interaction handlers');
  it.todo('handleNodeClick — Task 05');
  it.todo('handleDelete — Task 05');
  it.todo('makeHandleDblClick — Task 05');
});
