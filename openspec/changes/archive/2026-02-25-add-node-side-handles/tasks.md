## 1. Add side handles to node components

- [x] 1.1 Add left-target, left-source, right-target, right-source handles with unique IDs to PersonNode.svelte
- [x] 1.2 Add left-target, left-source, right-target, right-source handles with unique IDs to SystemNode.svelte
- [x] 1.3 Add left-target, left-source, right-target, right-source handles with unique IDs to ContainerNode.svelte
- [x] 1.4 Add left-target, left-source, right-target, right-source handles with unique IDs to ComponentNode.svelte

## 2. Update existing handles with IDs

- [x] 2.1 Add `id="top-target"` to existing top handles and `id="bottom-source"` to existing bottom handles across all four node components

## 3. Verify

- [x] 3.1 Run `npm run check` to confirm no type errors
- [x] 3.2 Run `npm run build` to confirm production build succeeds
